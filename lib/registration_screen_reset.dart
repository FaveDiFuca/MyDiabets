import 'package:flutter/material.dart';

class RegistrationScreenReset extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registrazione'),
      ),
      body: Center(
        child: Text('Registrazione in corso...'),
      ),
    );
  }
}